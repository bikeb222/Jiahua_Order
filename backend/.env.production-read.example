ORDER_ENV=ProductionReadOnly
ORDER_READ_DB_CONNECTION=DSN=test;DATABASE=omsdata;ApplicationIntent=ReadOnly;
ORDER_ENABLE_APP_WRITE=false
ORDER_ALLOW_PRODUCTION_READ=true

# Keep production writes disabled until the write process is verified against
# legacy OMS before/after snapshots for every affected table.
ORDER_ENABLE_PRODUCTION_OMS_WRITE=false
ORDER_SO_MIN_NUMBER=9438
